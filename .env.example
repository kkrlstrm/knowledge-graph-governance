# Provenance owner stamped on written nodes (falls back to $USER, then "kgg").
KGG_OWNER=

# Only needed for the neo4j backend (pip install knowledge-graph-governance[neo4j]).
# The backend DSN is the URI (e.g. neo4j://127.0.0.1:7687); credentials come from env.
NEO4J_USER=neo4j
NEO4J_PASSWORD=
